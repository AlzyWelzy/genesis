"""Business features, one package per feature.

Intentionally empty. Each feature owns its router, service, repository, models
and schemas in a single directory, so a change to one feature is a diff in one
place and a feature can be extracted into its own service by moving a folder.

See ``README.md`` in this directory for the required structure and the
responsibility of each file.
"""
