"""Feature tests, one package per module.

Mirrors :mod:`app.modules`. Each feature gets
``tests/modules/<feature>/`` containing its own ``conftest.py`` plus
``test_service.py``, ``test_repository.py``, ``test_router.py`` and
``test_schemas.py``.

Empty until the first feature module exists.
"""
